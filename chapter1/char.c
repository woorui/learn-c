#include <stdio.h>

int main(int argc, char const *argv[])
{
    // int c;
    // while ((c = getchar()) != EOF)
    // {
    //     putchar(c);
    // }

    // 1-7 打印 EOF
    printf("EOF = %d", EOF);
    return 0;
}
